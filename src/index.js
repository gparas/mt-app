import React from 'react';
import ReactDOM from 'react-dom';
import DefaultLayout from './layouts/Default';

const rootElement = document.getElementById('root');
ReactDOM.render(<DefaultLayout />, rootElement);
